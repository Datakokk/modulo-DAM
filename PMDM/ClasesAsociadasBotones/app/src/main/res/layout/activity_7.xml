<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center_horizontal"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ImageView
        android:id="@+id/btn_back"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/baseline_arrow_back_24"
        android:layout_gravity="end"
        android:padding="16dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="16dp"/>

    <!-- Campo para insertar la cantidad de metros -->
    <EditText
        android:id="@+id/et_input_meters"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Introduce el número de metros"
        android:inputType="numberDecimal"
        android:minHeight="48dp"
        android:padding="12dp"/>

    <Button
        android:id="@+id/btn_meters_to_KM"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="convertir metros a Km"/>

    <!-- Espacio adicional entre secciones -->
    <TextView
        android:id="@+id/tv_meters_to_km"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:hint="Aqui aparecen los Km"
        android:textSize="24sp" />

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#fff"
        android:layout_marginTop="32dp"/>

    <!-- Campo para insertar la cantidad de metros -->
    <EditText
        android:id="@+id/et_input_celsius"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Introduce los grados °C"
        android:inputType="numberDecimal"
        android:minHeight="48dp"
        android:padding="12dp"/>

    <Button
        android:id="@+id/btn_celsius_to_Fahrenheit"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="convertir celsius a fahrenheit"
        android:backgroundTint="@android:color/holo_blue_light"
        android:textColor="#000"/>

    <TextView
        android:id="@+id/tv_celsius_to_fahrenheit"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:hint="Aqui aparecen los grados °F"
        android:textSize="24sp" />

</LinearLayout>